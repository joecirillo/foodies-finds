name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          direct_prompt: |
            Review this pull request. Focus on correctness bugs, and flag any
            deviations from the conventions in CLAUDE.md and AGENTS.md
            (single responsibility, no `any` types, mobile-first UI/UX rules,
            self-documenting code with no unrequested comments). Post the
            review as a single PR comment.
